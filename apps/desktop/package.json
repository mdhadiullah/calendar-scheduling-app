{
  "name": "@calendar-app/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Windows/macOS desktop shell for the Calendar & Scheduling application, built with Tauri. Wraps the same React web app (apps/web) so the desktop, web, and (via the shared API) mobile clients stay in sync.",
  "scripts": {
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.6.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0"
  }
}
